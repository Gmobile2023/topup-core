using System.Collections.Generic;
using System.Threading.Tasks;
using HLS.Paygate.Gw.Domain.Entities;
using HLS.Paygate.Gw.Model.Dtos;
using HLS.Paygate.Shared;

namespace HLS.Paygate.Gw.Domain.Repositories
{
    public interface ISystemRepository
    {
        //Task<List<ServiceConfigDto>> GetServiceConfig(int serviceId, int categoryId, int productId);
        //Task<List<ServiceConfigDto>> GetServiceConfig(string serviceCode, string categoryCode);
        //Task<Partners> GetPartnerByCode(string code, PartnerStatus status);
        //Task<Categoryses> GetCategoryByCode(string code, bool checkStatu = false);

        //Task<Games> GetGame(string code, bool checkStatus);
        //Task<GameDto> GetGameProvider(string gamecode, string providercode, bool checkStatus);
        //Task<Providers> GetProvider(string code, bool checkStatus);

    }
}